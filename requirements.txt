# System dependency (not pip-installable): ffmpeg must be present on the
# machine for real video-bearing datasets. lerobot's video decoder
# (torchcodec) only checks that it's *importable*, not that its native lib
# can actually dlopen -- without system ffmpeg, decoding a real dataset's
# video crashes at runtime even though every synthetic-fixture test (which
# never decodes real video) passes. Install via `brew install ffmpeg` (macOS)
# or `sudo apt update && sudo apt install ffmpeg` (Ubuntu/Debian, the actual
# deployment target) before running against real datasets.
lerobot==0.4.4
numpy==2.0.2
scipy==1.17.1
opencv-python-headless==4.12.0.88
openai==1.109.1
ikpy==4.0.0
pydantic==2.10.3
PyYAML==6.0.2
openpyxl==3.1.5
pytest==8.3.4
h5py==3.12.1
tensorflow==2.18.0
