[project]
name = "vla-data-pipeline"
version = "0.1.0"
description = "VLA robotics dataset registry, onboarding, conversion, and cleaning/alignment pipeline"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["embodied_datasets/scripts/process_scripts"]
testpaths = ["embodied_datasets/scripts/process_scripts/tests"]
